<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "MovieFeeder :: Daniel Ravina" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div class="row">
  <div class="columns small-12">
    <header>
      <% if current_user %>
        <h6>Logged in as <%= current_user.to_s.titleize %> <a href="/logout">Logout</a></h6>
      <% end %>
      <h1>Movies Feeder <small>By Daniel Ravina</small></h1>
      <hr>
    </header>
      <%= yield %>
      <%= javascript_include_tag "application" %>
    </div>
    </div>
  </body>
</html>